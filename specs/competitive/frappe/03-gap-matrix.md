# Frappe Framework vs Frame — gap matrix

> Analysis date: 2026-08-02 · Analyst: Claude session for tushard@unops.org
> Sources fetched this session: see `01-profile.md` header (frappe@43666ea; Frame repo at working tree, master).
> Shelf life: pricing/changelog claims stale after ~1 quarter.

Status legend: superior · partial · absent · planned (PRD-only) · blocked by design (see the competitive-analysis skill for definitions). Frappe paths are in the session-scratchpad clone @43666ea; Frame paths are repo-relative.

| Their feature | Frame module / PRD | Status | Evidence |
|---|---|---|---|
| DocType metadata model (typed fields, validation, naming, select options, defaults) | `functions/lib/blueprint/model.py` (FieldDef, ValidationRule, Naming, SelectOption); PRD 01 BP-* | partial | Frame's metaschema is built and strict-validated, but far narrower than DocField's 80 properties (no fetch-from, no virtual fields, no masks yet); conditional properties are specified (BP-3a) on the shared grammar rather than Frappe's `depends_on` JS-ish strings |
| Child tables (parent/parenttype/parentfield; permissions delegated wholly to parent) | `functions/lib/blueprint/model.py` ChildCollection; `functions/lib/rows/children.py`; PRD 01, PM-3 | superior (design) | Frappe *clears* child permission rows and re-checks the parent (`frappe/permissions.py:806-900`); Frame gives a child collection its own rules inside the parent ceiling, applied "last and unconditionally" (`functions/lib/permissions/evaluate.py:115-193`), plus its own views/API per vision §3 — the capability Frappe's Report View special-case cannot express |
| Role permissions, 14 rights incl. submit/cancel/amend | `functions/lib/permissions/model.py` Action enum; PRD 05 PM-1 | partial | Frame's action vocabulary is smaller and has no submit/cancel/amend or print/email/import rights; deliberate for now, but export/import rights exist in Frappe and matter for governance (PM-13 exposure thinking) |
| Field-level access via integer `permlevel` bands | field-set rules in `functions/lib/permissions/evaluate.py` (`_fields_for`), typed restricted stubs in `trim.py`; PM-2 | superior | An integer band forces fields into a single linear sensitivity order per DocType; Frame's rules name field sets directly and trim server-side with restricted stubs — vision §2 calls this out and the code matches |
| User Permissions (per-user equality allow-lists on Link values) | PM-2a principal allow-lists resolved inside grammar conditions (`evaluate.py:281-290`) | superior (design) | Frappe: equality-only, fail-open when absent, strictness a global toggle (`frappe/permissions.py:351-478`; forum thread "design of User Permissions is dangerous", discuss.frappe.io/t/109103); Frame: allow-lists are inputs to compiled row conditions in one evaluator — any operator, cross-field, no second path |
| Attribute-based access beyond equality (only via `has_permission` + `permission_query_conditions` hooks returning raw SQL, kept in sync by hand) | `functions/lib/grammar/compile_query.py` + `functions/lib/permissions/evaluate.py`; PM-2, PM-4 | superior (design) | Frappe's real ABAC is two implementations in two places (`frappe/permissions.py:481-498`; `frappe/model/db_query.py:1332-1360`); Frame compiles one declarative rule set to both single-row evaluation and query trimming from one AST. This is the core bet — it exists in code, though the rule vocabulary is young |
| Per-document sharing (DocShare as an OR-path around role perms) | none; N8 "no per-row grants" | blocked by design | `frappe/share.py`, `frappe/permissions.py:177-209` vs `specs/frame-prds/product-vision-frame.md` N8 and PM-2a: access from compiled rules only, which keeps access review complete by construction. User outcome (ad-hoc inclusion) is served by principal allow-lists inside rules |
| Per-DocType controller code (validate/on_update hooks, static `get_list` query shaping, whitelisted doc methods) | none; CLAUDE.md break #1 | blocked by design | ~320 controller files in the clone (`frappe/*/doctype/*/*.py`); `frappe/api/v2.py:118-183` invites per-DocType Python. Frame: no per-Blueprint router/file, enforced by `tools/fitness/architecture.test.ts`; the outcome (validation, computed behavior) is declarative Blueprint metadata + shared grammar |
| Generic REST API (CRUD, filtered lists, count, meta, bulk with async threshold, discovery endpoints) | `functions/api/routers/rows.py` (query/list/import/export/deltas/children), `blueprints.py`, `docs.py`; typed client `src/api/client.ts` | partial | Frame's generic surface exists and publishes per-Blueprint-version OpenAPI (`functions/api/routers/docs.py:53-80`) — something Frappe does not do (its v2 "discovery" endpoints are method listings, not typed schemas). Missing vs Frappe: bulk update/delete with 202+job semantics, doc-method execution, RPC surface |
| Workflow engine (states→docstatus, role-gated transitions, safe_eval conditions, transition tasks, self-approval block, bulk actions) | `functions/lib/blueprint/model.py` WorkflowState/WorkflowTransition; PRD 04 AU-* | partial | Frame's state/transition *model* exists in metadata; the executing engine, actions and event contract are PRD-only (PRD 04). Frappe's conditions are untyped Python strings (`frappe/model/workflow.py:96-100`); Frame's are specified as grammar ASTs (shared-grammar definition, `00-prd-index.md`) — better once built, absent until then |
| docstatus finality (draft/submitted/cancelled, amend-with-chain) | Lifecycle (freeze/retention/archival) in `functions/lib/blueprint/model.py`; PRD 01 | partial | Frame has freeze/lifecycle policies, not an amend chain. Deliberately not copied: amend chains are ERP transaction semantics; Frame's registers version rows (`functions/lib/rows/audit.py`, deltas) rather than superseding documents. Watch item, not a gap to close |
| Versioning: field-level diffs incl. child rows, impersonator attribution | `functions/lib/rows/audit.py`, `functions/lib/rows/deltas.py`; Blueprint versions in `functions/lib/blueprint/` (version field, compile-per-version); PRD 01, PM audit | partial | Both exist in code; Frappe's Version additionally records impersonated_by/audit_user (`version.py:39-47`) — worth stealing into Frame's audit entries when impersonation/service identities arrive |
| Customization overlay surviving upgrades (Property Setter, Custom Field, Custom DocPerm, Customize Form) | no analog; nearest: Blueprint versioning + promotion ladder (BP-15..19) | absent | No overlay concept in `functions/lib/blueprint/` or PRD 01: a team adopting a catalog Blueprint cannot add a local field or relabel without a new Blueprint version or a fork. Frappe's mechanism (`property_setter.py`, `customize_form.py`) is the proof this is what makes a shared metadata core adoptable. → 04-opportunities O2 |
| Server Script (user Python in restricted runtime, off by default) | none; N5 closed action vocabulary | blocked by design | `frappe/core/doctype/server_script/server_script.py:290` (`is_safe_exec_enabled` site-config gate) — Frame's vision N5 cites exactly this gating as evidence the pressure is real; the answer stays the code-first action library (AU-3, AU-3a), never a user runtime |
| Realtime: separate socket process, permission decided by callback into app | planned; vision §5 (Frame-owned realtime tier, rooms as permission decisions); PM-4, PRD 02/05 | planned (PRD-only) | `realtime/handlers.js:12-25` is the reference implementation Frame's vision explicitly says to copy; nothing in `src/` or `functions/` implements rooms yet (`grep -ril realtime` — no hits outside venv) |
| Desk UI: metadata-driven form/list/kanban/calendar/tree views | `src/grid/` (FrameGrid), `src/registers/`; PRD 02 GR-* | partial | Frame's grid + register pages exist; view morphing (board/calendar/Gantt) is PRD-only (GR-*). Note Frappe's own best editable grid (Report View) lacks paste/fill/undo per vision §2 — the grid is Frame's ground, not Frappe's |
| Web Forms (incl. private links, v16.28) | PRD 03 FM-* | planned (PRD-only) | No forms code in `src/` or `functions/api/routers/`; PRD 03 covers internal/external forms, conditional logic, child sections |
| Report Builder / query reports / report snapshots (v16.27) | PRD 06 RP-* | planned (PRD-only) | No reporting code; PRD 06 specifies reports/dashboards and the Prism boundary (N2) |
| Print formats / document generation | PRD 07 DG-* | planned (PRD-only) | `frappe/printing/` exists in clone; Frame's PRD 07 covers templates incl. child iteration |
| Four DB backends (MariaDB/Postgres/SQLite/DuckDB), site-per-tenant bench | Firestore system-of-record + Postgres derived replica; vision §5 | absent (deliberate architecture difference, not a roadmap gap) | `frappe/database/{mariadb,postgres,sqlite,duckdb}/` vs vision §5 invariant (Firestore authoritative, Postgres rebuildable). Frappe's per-backend DDL/sequence code is named prior art for Frame's Postgres side — mining opportunity, not parity |
| Permission debug trace ("why was this denied") | Decision/Annotation objects in `functions/lib/permissions/model.py:84-136`; PM-5 transparency annotations | partial | Frappe explains denials to developers (`frappe/permissions.py:43-77`); Frame's annotations explain *trimming* to viewers but there is no steward-facing "evaluate as user X, show the trace" surface → 04-opportunities O1 |

## Reading the matrix

**Where Frame is genuinely behind.** Three places. First, metaschema breadth: DocField's 80 properties versus Frame's young FieldDef — most of the difference is fetch-from-link, virtual/computed display fields, and conditional-property coverage, which PRD 01 specifies but code does not yet carry. Second, API completeness: Frappe's bulk operations with async job semantics and doc-method execution have no Frame counterpart yet. Third — and the only *conceptual* gap rather than a not-built-yet gap — the customization overlay. Frappe demonstrates that a shared metadata core is only adoptable at scale because sites can diff against it without forking; Frame's promotion ladder currently has no equivalent seam, and that is a real threat to catalog adoption, not a cosmetic one.

**Where "behind" is really "specified, unbuilt".** Workflow execution, forms, reporting, document generation, realtime rooms, view morphing. Each has a PRD and a deliberate phase; the matrix confirms no silent scope holes — every headline Frappe capability lands in a numbered PRD. The one addition worth folding into an existing PRD rather than a new one: impersonator/service-identity attribution in audit entries (Frappe's `Version.set_impersonator`), which belongs in PRD 05's audit requirements.

**Where Frappe's mechanism is refused on purpose.** Per-DocType controller files, Server Script, and DocShare are the three mechanisms Frame's invariants forbid — and in each case Frappe's own code concedes the danger (Server Script disabled by default behind a site-config flag; the forum's fail-open User Permission incident; controllers making "zero code" a marketing claim rather than an architectural fact). Frame's versions of these outcomes — declarative rules in one evaluator, a closed action vocabulary, allow-lists inside compiled conditions — are the product thesis. The fitness suite is what keeps a future contributor from helpfully reintroducing the Frappe shape.

**The two-implementations finding is the headline.** Frappe's declarative permissions stop at role × permlevel × owner × equality allow-lists; everything genuinely attribute-based lives in paired hooks — a Python predicate for single documents and a raw-SQL fragment for lists — that nothing keeps consistent. Frame's single compiled rule set feeding both `evaluate_row` and `compile_query` is, verified against the actual Frappe source this session, a real architectural advance over the reference implementation rather than a marketing restatement of it.

## Post-synthesis correction (2026-08-02, pipeline phase 4)

The "Customization overlay" row's status of **absent** is wrong against the
current PRD set: BP-19 (PRD 01) already specifies bind-not-copy catalog
adoption *with* workspace overlays — added namespaced fields plus
tighten-only property and permission overrides, merged once at metadata
load — and PRD 01's decisions log records it as resolved August 2026
against exactly this Frappe evidence. The row is correct only about code
(`functions/lib/blueprint/` has no overlay implementation yet). What this
run genuinely added is BP-27, overlay *convergence reporting*.
